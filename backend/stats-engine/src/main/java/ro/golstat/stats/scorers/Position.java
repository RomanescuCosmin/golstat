package ro.golstat.stats.scorers;

/** Pozitia jucatorului — decide baseline-ul de goluri/90 spre care se face shrinkage. */
public enum Position {
    FORWARD,
    MIDFIELDER,
    DEFENDER,
    GOALKEEPER
}
